# Ariana Birthday Site 💖

Это статический сайт (HTML/CSS/JS) — работает на телефоне и ноутбуке, без сборки.

## Как поменять имена / текст
Открой `script.js` и измени:

```js
const CONFIG = {
  herName: "Ариана",
  herNameCute: "Арианочка",
  yourName: "твой …",
  dateLabel: "С Днём Рождения"
};
```

Текст письма меняется в `index.html` в секции **Письмо тебе**.

## Как запустить локально
Просто открой файл `index.html` двойным кликом.

## Как выложить на GitHub Pages
1) Создай репозиторий на GitHub (например `ariana`).
2) Залей файлы в репозиторий (commit + push).
3) GitHub → **Settings** → **Pages** → **Deploy from a branch** → Branch: `main`, Folder: `/ (root)`.
4) Ссылка будет вида: `https://<username>.github.io/<repo>/`

> Файлы большие (видео/музыка). Если GitHub будет ругаться — проверь, что каждый файл меньше 100MB.
